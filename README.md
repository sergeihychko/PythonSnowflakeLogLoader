# PythonSnowflakeLogLoader
An application to load game log files into a local instance of a Snowflake database.


Dependencies:
pip install --upgrade localstack

References:
https://docs.localstack.cloud/snowflake/getting-started/
